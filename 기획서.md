
## 📱 Weather Time Machine - AI 구현 가이드

### 프로젝트 컨텍스트
```
프로젝트명: Weather Time Machine
기술 스택: React Native Expo (SDK 54), TypeScript, Expo Router
타겟 플랫폼: iOS, Android
아키텍처: 단일 화면 앱 (Single Screen Application)
```

## 1. **구현 요구사항 명세**

### 1.1 핵심 기능 요구사항
```yaml
필수 기능:
  - 오늘 날씨와 작년 같은 날 날씨를 한 화면에 비교 표시
  - 온도 차이를 시각적으로 강조 (색상, 화살표, 숫자)
  - Pull-to-refresh로 데이터 새로고침
  - 위치 자동 감지 (권한 거부 시 서울 기본값)
  
Mock 데이터 구조:
  today:
    date: string (YYYY-MM-DD)
    temperature: number
    feelsLike: number
    weatherCondition: 'sunny' | 'cloudy' | 'rainy' | 'snowy'
    humidity: number
    windSpeed: number
    aqi: number (미세먼지)
  
  lastYear:
    date: string (YYYY-MM-DD) 
    temperature: number
    feelsLike: number
    weatherCondition: string
    humidity: number
    windSpeed: number
    aqi: number
```

### 1.2 UI 컴포넌트 구조
```typescript
// 컴포넌트 계층 구조
<MainScreen>
  <StatusBar /> // 투명 처리
  <GradientBackground /> // 날씨 상태 기반 그라데이션
  <SafeAreaView>
    <LocationHeader />
      - 현재 위치 (예: 서울시 강남구)
      - 오늘 날짜 (11월 19일 화요일)
    
    <ScrollView refreshControl>
      <ComparisonCard>
        <TodayWeather />
        <ComparisonIndicator /> // 온도 차이 표시
        <LastYearWeather />
      </ComparisonCard>
      
      <FeelsLikeComparison />
      <WeatherDetails /> // 습도, 바람, 미세먼지
    </ScrollView>
  </SafeAreaView>
</MainScreen>
```

## 2. **스타일 가이드**

### 2.1 색상 시스템
```javascript
const WeatherColors = {
  warmer: {
    primary: '#FF6B6B',    // 작년보다 따뜻함
    gradient: ['#FF6B6B', '#FF8787']
  },
  colder: {
    primary: '#4ECDC4',    // 작년보다 추움
    gradient: ['#4ECDC4', '#44A39F']
  },
  similar: {
    primary: '#95A5A6',    // 비슷함 (±2°C)
    gradient: ['#95A5A6', '#7F8C8D']
  },
  weather: {
    sunny: '#FFD93D',
    cloudy: '#BDC3C7',
    rainy: '#3498DB',
    snowy: '#ECF0F1'
  }
}
```

### 2.2 타이포그래피
```javascript
const Typography = {
  mainTemperature: {
    fontSize: 48,
    fontWeight: 'bold'
  },
  temperatureDiff: {
    fontSize: 24,
    fontWeight: '600'
  },
  label: {
    fontSize: 14,
    opacity: 0.7
  },
  location: {
    fontSize: 18,
    fontWeight: '500'
  }
}
```

## 3. **상태 관리 설계**

### 3.1 앱 상태 구조
```typescript
interface AppState {
  location: {
    city: string;
    district: string;
    coords: { latitude: number; longitude: number };
  };
  weatherData: {
    today: WeatherData;
    lastYear: WeatherData;
    lastUpdated: Date;
  };
  ui: {
    isLoading: boolean;
    isRefreshing: boolean;
    error: string | null;
  };
  preferences: {
    temperatureUnit: 'C' | 'F';
    useLocation: boolean;
  };
}
```

### 3.2 Hook 구현 가이드
```typescript
// hooks/use-weather-comparison.ts
/*
구현 내용:
1. Mock 데이터 로드
2. 위치 정보 처리
3. 온도 차이 계산
4. 새로고침 로직
5. 로딩/에러 상태 관리
*/

// hooks/use-location.ts
/*
구현 내용:
1. Expo Location 권한 요청
2. 현재 위치 가져오기
3. Reverse Geocoding으로 도시명 변환
4. 권한 거부 시 서울 기본값 설정
*/
```

## 4. **애니메이션 명세**

### 4.1 Reanimated 애니메이션
```javascript
애니메이션 요소:
1. 진입 애니메이션:
   - FadeInDown: 카드 순차적 등장
   - 지연시간: 각 카드 100ms 간격

2. 온도 차이 표시:
   - 화살표 회전: 상승(45deg), 하강(-45deg)
   - 색상 전환: interpolateColor 사용
   - Spring animation 적용

3. Pull-to-refresh:
   - 회전하는 새로고침 아이콘
   - 스케일 효과

4. 날씨 아이콘:
   - 미세한 float 애니메이션 (2초 주기)
```

## 5. **파일 구조 매핑**

```
app/
  _layout.tsx              # Stack 네비게이터 (탭 제거)
  index.tsx                # 메인 화면
  
components/
  weather/
    LocationHeader.tsx     # 위치/날짜 헤더
    ComparisonCard.tsx     # 메인 비교 카드
    WeatherCard.tsx        # 개별 날씨 카드 (오늘/작년)
    ComparisonIndicator.tsx # 온도 차이 인디케이터
    WeatherDetails.tsx     # 상세 정보 섹션
  ui/
    GradientBackground.tsx # 동적 배경
    LoadingSkeleton.tsx    # 스켈레톤 로더
    
hooks/
  use-weather-comparison.ts
  use-location.ts
  use-animated-temperature.ts
  
services/
  mock-weather-data.ts     # Mock API 응답
  weather-service.ts       # API 인터페이스 (준비)
  
utils/
  temperature-utils.ts     # 온도 계산/변환
  date-utils.ts           # 날짜 포맷팅
  
constants/
  weather-theme.ts        # 날씨 전용 테마 확장
```

## 6. **구현 우선순위**

### Phase 1 - MVP (현재 목표)
```
1. 기본 레이아웃 구성 ✓
2. Mock 데이터 연동 ✓
3. 온도 비교 카드 구현 ✓
4. 색상 시스템 적용 ✓
5. Pull-to-refresh ✓
```

### Phase 2 - 향상
```
6. 애니메이션 추가
7. 위치 권한 처리
8. 로컬 스토리지 캐싱
9. 에러 처리 개선
10. 성능 최적화
```

## 7. **테스트 시나리오**

```yaml
필수 테스트:
  - 앱 첫 실행 시 3초 내 데이터 표시
  - Pull-to-refresh 작동 확인
  - 온도 차이에 따른 색상 변경
  - 다크모드/라이트모드 전환
  - 위치 권한 거부 시 폴백
  
엣지 케이스:
  - 네트워크 오류 상황
  - 작년 데이터 없을 때
  - 극단적 온도 차이 (±20°C)
```

## 8. **API 준비 사항**

```typescript
// 향후 연동할 API 엔드포인트 구조
interface WeatherAPI {
  getCurrentWeather: (lat: number, lon: number) => Promise<WeatherData>;
  getHistoricalWeather: (lat: number, lon: number, date: string) => Promise<WeatherData>;
}

// Mock에서 실제 API로 전환 시 변경 최소화를 위한 인터페이스
```

## 9. **성능 최적화 지침**

```
- React.memo 사용: 날씨 카드 컴포넌트
- useMemo: 온도 차이 계산
- Lazy loading: 상세 정보 섹션
- 이미지 최적화: 날씨 아이콘 SVG 사용
- 폰트 최적화: system 폰트 우선 사용
```

## 10. **접근성 고려사항**

```
- VoiceOver/TalkBack 레이블 추가
- 색맹 모드: 색상 외 패턴/아이콘 추가
- 최소 터치 영역: 44x44pt
- 고대비 모드 지원
```

---

### 구현 시작 명령
```bash
# 프로젝트 초기화 (이미 완료됨)
npx create-expo-app WeatherTimeMachine --template

# 필요 패키지 설치
npx expo install expo-location expo-linear-gradient react-native-reanimated

# 개발 서버 시작
npx expo start
```

