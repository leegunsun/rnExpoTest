/**
 * 공유 라우트 - 사용자 프로필 화면 (검색 탭용)
 *
 * 이 파일은 (home)/[user].tsx와 동일한 코드입니다.
 * 실제 프로젝트에서는 공통 컴포넌트를 분리하여 재사용합니다.
 */

import { UserProfileScreen } from "@/components/route-groups-guide/UserProfileScreen";

export default UserProfileScreen;
